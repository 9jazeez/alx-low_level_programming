#include "4-sum.h"
#include "4-sum.h"
#include "3-function_like_macro.h"
#include <stdio.h>

int main(void)
{
	int a;
	int r;
	int c;

	r = ABS(-98) * 10;
	a = 98 * 10;
	c = ABS(2);
	printf("%d , %d , %d\n", SUM(c, r), a, c);

	return (0);
}
